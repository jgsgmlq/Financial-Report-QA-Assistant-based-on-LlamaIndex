# 复制本文件为项目根目录下的 `.env` 后填入真实值：
#   cp .env.example .env
#
# LlamaCloud / LlamaParse：高保真 PDF → Markdown（表格、双栏）
# 获取密钥：https://cloud.llamaindex.ai/
#
# 若不配置：请在 configs/config.yaml 中将 parser.use_llamaparse 设为 false，
# 系统将使用本地基础解析器。

# 将下一行整段替换为真实密钥；若保持以 llx-your 开头的占位符，程序会视为未配置并降级本地解析
LLAMA_CLOUD_API_KEY=llx-your-key-replace-me
